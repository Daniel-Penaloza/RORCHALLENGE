<center><h1> Explore by Category </h1></center>
<i> <center> Exploring <%= @categories.count %> categories.</i></center>

   <% @categories.each do |cat| %>
    <div class="wellc">
    <div class="wrapper">
      <div class="box a"><%= link_to(image_tag("http://www.livecircular.com/wp-content/uploads/2016/10/other-electronic-reviews.jpg"), category_path(cat)) %></div>
      <div class="box b"><h2><%= cat.name.capitalize %> </h2> <br>
      
      <%if cat.products.any?%>
        <b>Sellers who have items in this category:</b> <br> <br>
        <ul>
          <% cat.products.each do |prod| %>
             <li><%= link_to prod.user.first_name, user_path(prod) %></li>
          <% end %>
        </ul>
      <% else %>
      <p> There are no products in this category yet!</p>
      <% end %>
        
      </div>
      <div class="box c"><%= link_to "View category", category_path(cat), class: "btn-actions cat-button-box-c" %></div>
      <div class="box d"><p>Products in this category: <%= cat.products.count %></p></div>
      <div class="box e"><%= link_to "Edit Category", edit_category_path(cat), class: "btn-actions cat-button" %> 
      <%= link_to "Delete", category_path(cat), method: :delete,
            data: { confirm: "Are you sure you want to delete this catgory?" }, class: "btn-actions cat-button"  %></div>
    </div>
  </div> 

     <% end %>
